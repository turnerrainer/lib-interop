def assistant(transcription):
    print(transcription)
	# handle locally?
	# if handle_locally:
	# what_to_say = decide_what_to_say
	# if not handle locally:
        # assistant = identify assistant(transcription)
	    # convert input to OVON
	    # send input to assistant (assistant)
	    # receive result
	# log result
    return("I heard" + transcription)
	
def handle_locally(transcription):
    return(False)
	
def identify_assistant(transcription):
    return("test")